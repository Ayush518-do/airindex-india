[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    slow: trains the fare model; deselect with -m "not slow"
filterwarnings =
    ignore::DeprecationWarning
    ignore:Using `httpx` with `starlette.testclient`
